# Write a program that calculates and displays the area of a triangle, knowing that: area = (base * height)/2

base = float(input("Base: "))
height = float(input("Height: "))

print(f"Area of triangle: {(base * height)/2}")
